#include <stdio.h>
#include <stdbool.h>
#include "dec_data.c"
#include "dec_complex.c"
#define MIN(X, Y) (((X) < (Y)) ? (X) : (Y))
#define MAX(X, Y) (((X) > (Y)) ? (X) : (Y))
extern int ve;
extern int ve2;
extern int last;
extern int next;
extern int suma;
extern int cont;
extern float r;
extern float i;
extern struct listadeint lx;
extern struct listadeint lx2;
extern struct listadeint lx3;
extern struct complejo c1;
